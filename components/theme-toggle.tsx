'use client'

/**
 * Previously toggled between light and dark themes. The app is now
 * light-only (dark was never fully designed) — this component renders
 * nothing so existing layout imports keep working without a user-facing
 * toggle button. Safe to delete along with any remaining imports.
 */
export function ThemeToggle() {
  return null
}
