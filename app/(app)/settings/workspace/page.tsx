import { PlaceholderSection } from '@/components/workspace/placeholder-section'

export const metadata = {
  title: 'Workspace settings',
}

export default function WorkspaceSettingsPage() {
  return (
    <PlaceholderSection
      title="Workspace settings"
      description="Rename, invite members, change type. Ships in Milestone 2."
    />
  )
}
