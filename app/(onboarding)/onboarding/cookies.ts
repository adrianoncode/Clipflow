/**
 * Shared cookie names for the onboarding wizard. Lives in its own file
 * because Next's 'use server' action files may only export async functions.
 */
export const ONBOARDING_ROLE_COOKIE = 'clipflow.onboarding_role'
